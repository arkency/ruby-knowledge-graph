node_kinds:
  - kind: person
    description: "speaker, attendee, organizer, developer"
  - kind: talk
    description: "conference talk, workshop, panel, lightning talk"
    attrs:
      - name: type
        values: [talk, workshop, panel, lightning-talk]
  - kind: event
    description: "conference, meetup, camp, community gathering"
    attrs:
      - name: format
        values: [in-person, remote, hybrid]
      - name: date
      - name: location
  - kind: company
    description: "company, organization"
    attrs:
      - name: industry
  - kind: tool
    description: "framework, library, language, platform"
    attrs:
      - name: category
        values: [framework, library, service, platform]
  - kind: concept
    description: "technical concept, pattern, methodology"
    attrs:
      - name: category
        values: [pattern, methodology, architecture, practice]
  - kind: project
    description: "software project, open-source product"
    attrs:
      - name: license
        values: [open-source, closed-source]
      - name: status
        values: [active, completed]
  - kind: resource
    description: "book, article, blog post, documentation, repository"
    attrs:
      - name: type
        values: [book, article, blog-post, documentation, repository]
  - kind: question
    description: "an audience question from Q&A, capturing the topic being asked about"
    attrs:
      - name: answer_summary
        description: "brief summary of the speaker's answer"

edge_relations:
  - relation: authored
    signature: "person --authored--> talk | resource"
  - relation: presented_at
    signature: "talk --presented_at--> event"
  - relation: attended
    signature: "person --attended--> event"
    attrs:
      - name: role
        values: [participant, speaker, organizer]
  - relation: sponsors
    signature: "company --sponsors--> event"
  - relation: about
    signature: "talk | question --about--> concept | tool | project | resource | person"
    hint: "what the talk or question covers"
  - relation: asked_at
    signature: "question --asked_at--> talk"
  - relation: works_at
    signature: "person --works_at--> company"
    attrs:
      - name: role
  - relation: works_on
    signature: "person --works_on--> project | tool"
    attrs:
      - name: role
  - relation: uses
    signature: "person | project --uses--> tool"
  - relation: has_skill
    signature: "person --has_skill--> concept | tool"
    attrs:
      - name: level
        values: [beginner, intermediate, expert]
  - relation: recommends
    signature: "person --recommends--> tool | concept | resource"
  - relation: related_to
    signature: "* --related_to--> *"
    hint: "last resort — use only when no specific relation fits"
